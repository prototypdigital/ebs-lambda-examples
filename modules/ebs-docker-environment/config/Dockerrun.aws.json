{
  "AWSEBDockerrunVersion": "1",
  "Image": {
    "Name": "${repository_url}:${environment}",
    "Update": "true"
  },
  "Ports": [
    {
      "ContainerPort": "${api_port}",
      "HostPort": "${api_port}"
    }
  ]
}
